export { Logger } from './Logger';

/**
 * @module LoggerService
 * 
 * ## Setup
 * - Create a singleton `new Logger()` at module-level.
 * 
 * ## Usage
 * - Call to log at granularity level: trace, debug, info, warn, error, etc.
 */
